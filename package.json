{
  "name": "jira-validation",
  "version": "1.0.0",
  "private": true,
  "description": "GitHub Action to validate JIRA ticket references in PRs",
  "scripts": {
    "test": "jest --verbose"
  },
  "devDependencies": {
    "jest": "^30.2.0"
  }
}
